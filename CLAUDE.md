# algorithm-study

혼자 쓰는 알고리즘 학습 리포지토리. 코드 리뷰어가 따로 없으므로 브랜치·PR 절차를 생략한다.

## 브랜치 전략 — main에서 직접 작업

- **`main`에서 바로 커밋하고 푸시한다.** 작업용 브랜치를 새로 파지 않는다.
- PR도 기본 절차가 아니다. 필요하면 그때 명시적으로 요청한다.
- `main`에는 브랜치 보호 규칙이 없다(= 실수한 커밋도 그대로 올라간다). 그래서 아래 **푸시 전 검증**이 절차상 유일한 안전망이다.

## 푸시 전 검증 (필수)

풀이 코드를 건드렸으면 **푸시 전에 컴파일 + 채점 하네스를 돌리고 결과를 보여준 뒤** 푸시한다. PASS를 확인하지 않은 채로 푸시하지 않는다.

```bash
cd java/26-q3
javac -encoding UTF-8 -d out $(find src -name "*.java")
java -cp out src.set01.Ex002_120923   # 파일별로 main()에 채점 하네스가 들어 있다
```

`out/`은 `.gitignore` 대상이다.

## 풀이 파일 규칙

- 경로: `java/26-q3/src/set<2자리 패키지번호>/Ex<3자리 순서>_<프로그래머스 문제ID>.java` (예: `src/set01/Ex002_120923.java`)
- `Ex` 뒤 3자리는 노션 `프로그래머스 문제 목록` DB의 **`순서` 컬럼**(1~76)이 정본이다
- 패키지는 순서 10개 단위로 끊는다 — `set01` = 1~10, `set02` = 11~20, …, `set08` = 71~76
- `package src.set<NN>;` — 디렉토리 구조와 일치시킨다
- 클래스 상단 주석에는 **문제 이름과 링크만** 둔다. 조건·유형을 먼저 보면 풀이 방향이 굳어지므로 키워드·레벨·제한사항은 파일에 적지 않는다
- 레벨·단원·키워드는 `java/26-q3/문제-목록.md` 표에서만 본다 (제한사항은 프로그래머스 링크가 정본)
- 클래스 하단: `// ===== 아래는 채점용. 풀이만 위에서 수정하면 됩니다 =====` 구분선 + `main()` + `check()`
- `check()`의 테스트 케이스는 프로그래머스 **공식 입출력 예**를 그대로 쓴다
- 풀이 당시 남긴 `느낀 점` 메모가 있으면 주석으로 함께 보존한다

## 커밋 / PR 메시지

정본은 `.claude/commands/commit.md`, `.claude/commands/pr.md`. 핵심만:

- 커밋 제목은 영어 Conventional Commits, 본문도 영어 불릿
- `Co-Authored-By`, `Generated by` 등 **AI attribution은 어디에도 넣지 않는다**
